package org.fc.brewchain.p22p.test

import org.fc.brewchain.p22p.core.Votes._
import org.fc.brewchain.bcapi.URLHelper
import java.net.URL
import org.fc.brewchain.p22p.node.Network
import org.fc.brewchain.p22p.node.PNode

//import scala.collection.Searching._

object TestSign {
  def main(args: Array[String]): Unit = {
//    val net = new Network("raft", "");
//    val kp = EncHelper.newKeyPair()
//    val signc = EncHelper.ecSign(kp.prikey, Array("111", "aaa", "ccc").mkString("|").getBytes)
//    println("signc=" + signc)
//    val bb = EncHelper.ecVerify(kp.pubkey, Array("111", "aaa", "ccc").mkString("|").getBytes, signc.getBytes)
//    println("verify=" + bb)
  }

}